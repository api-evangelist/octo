naftiko: 1.0.0-alpha2
info:
  label: Open Connectivity for Tours, Activities, and Attractions OCTO API Specification — Supplier
  description: 'Open Connectivity for Tours, Activities, and Attractions OCTO API Specification — Supplier. 1 operations.
    Lead operation: Open Connectivity for Tours, Activities, and Attractions Get Supplier. Self-contained Naftiko capability
    covering one Octo business surface.'
  tags:
  - Octo
  - Supplier
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    OCTO_API_KEY: OCTO_API_KEY
capability:
  consumes:
  - type: http
    namespace: octo-supplier
    baseUri: https://api.example.com/octo
    description: Open Connectivity for Tours, Activities, and Attractions OCTO API Specification — Supplier business capability.
      Self-contained, no shared references.
    resources:
    - name: supplier
      path: /supplier
      operations:
      - name: get
        method: GET
        description: Open Connectivity for Tours, Activities, and Attractions Get Supplier
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.OCTO_API_KEY}}'
  exposes:
  - type: rest
    namespace: octo-supplier-rest
    port: 8080
    description: REST adapter for Open Connectivity for Tours, Activities, and Attractions OCTO API Specification — Supplier.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/supplier
      name: supplier
      description: REST surface for supplier.
      operations:
      - method: GET
        name: get
        description: Open Connectivity for Tours, Activities, and Attractions Get Supplier
        call: octo-supplier.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: octo-supplier-mcp
    port: 9090
    transport: http
    description: MCP adapter for Open Connectivity for Tours, Activities, and Attractions OCTO API Specification — Supplier.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: open-connectivity-tours-activities-and
      description: Open Connectivity for Tours, Activities, and Attractions Get Supplier
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: octo-supplier.get
      outputParameters:
      - type: object
        mapping: $.
